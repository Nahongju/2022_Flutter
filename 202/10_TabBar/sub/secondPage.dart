import 'package:flutter/material.dart';

class SecondtApp extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('두 번째 페이지')
        )     // Center
      )     // Container
    );      // Scaffold
  }
}
